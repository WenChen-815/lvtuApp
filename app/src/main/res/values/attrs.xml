<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="DraggableConstraintLayout">
        <!--是否需要自动吸边-->
        <attr name="customIsAttach" format="boolean" />
        <!--是否可拖曳-->
        <attr name="customIsDrag" format="boolean" />
    </declare-styleable>

    <declare-styleable name="AIAssistantConstrainLayout">
        <!--是否需要自动吸边-->
        <attr name="aiCustomIsAttach" format="boolean" />
        <!--是否可拖曳-->
        <attr name="aiCustomIsDrag" format="boolean" />
    </declare-styleable>
</resources>